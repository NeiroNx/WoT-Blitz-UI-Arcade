action UpdateMedalsVisibility(float contentWidth, float scrollViewWidth, float positionX)
{
  if (itemsCount > 0)
  {
    BroadcastEvent("UpdateOpacity", "./Medals", (contentWidth / float(itemsCount)), scrollViewWidth, positionX);
  }
}